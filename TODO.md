# TODO - Fix reload saat scroll

- [ ] Update `styles.css` agar `html, body` tidak ikut scroll (overflow hidden) dan hanya konten `main` yang boleh scroll.
- [ ] (Opsional setelah uji) Jika masih terjadi, lakukan penyesuaian selector scroll container (mis. `#sidebar`/`main.custom-scrollbar`).
- [ ] Uji di Chrome: buka halaman, scroll pakai mouse wheel & drag scrollbar, pastikan tidak ada refresh.

